# Binaries
bin/
*.exe

# Release artifacts (generated by scripts/build-release.sh; published to GitHub Releases)
dist/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# Test
coverage.out

# Local dev
.direnv/
.clawpatch/

# Web UI
web/node_modules/
web/.next/
web/tsconfig.tsbuildinfo
web/package-lock.json
web/pnpm-lock.yaml

# build artifacts (root-level binaries only, not cmd/claw-bridge/ source)
/claw-bridge
/claw-bridge-linux
/claw-bridge-linux-amd64
bin/

# Embedded web UI build output (generated by make build-web)
internal/webui/out/
web/out/
memory/
bootopt

.demovoice/runs
.demovoice/eval.yaml

# Docker dev environment (contains secrets — never commit)
docker/hub.dev.yaml
tmp/
